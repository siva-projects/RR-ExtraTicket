

export async function authMiddleware() {
    // console.log("middleware got hit");
    //   const user = null;
    //   if (!user) {
    //     throw redirect("/login");
    //   }
    // set context if needed, example:
    // context.set(userContext, user);

}
